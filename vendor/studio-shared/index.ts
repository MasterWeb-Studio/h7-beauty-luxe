// Vendored lean barrel for @studio/shared (scaffolded project runtime).
// Only re-exports the subset used by the scaffolded site — no Claude SDK,
// no Supabase admin client, no orchestrator/agent code.
export * from './storage.js';
export * from './specs/index.js';
export * from './animation-presets.js';
